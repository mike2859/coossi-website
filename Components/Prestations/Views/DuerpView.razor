@using Coossi.Blazor.Components.Shared
@using Coossi.Blazor.Data.Interfaces
@using Coossi.Blazor.Data.Models.Prestations
@inject IContentStore Store
@inject NavigationManager Nav

@code {
    [Parameter] public string Slug { get; set; } = "";
    private DuerpPage? Model;

    protected override async Task OnParametersSetAsync()
        => Model = await Store.GetAsync<DuerpPage>($"prestations/{Slug}");

    string Canonical => $"{Nav.BaseUri.TrimEnd('/')}/prestation/{Slug}";
}

@if (Model is null) { <section class="content-section"><div class="container text-muted">Chargement…</div></section> }
else {
  <Seo Title="@Model.Seo.MetaTitle" Description="@Model.Seo.MetaDescription" Canonical="@Canonical" OgImage="@(Model.Seo.OgImage ?? "images/og-prestations.webp")" />
  @* <PageHeader Title="@Model.Hero.Title" Lead="@Model.Hero.Subtitle" /> *@

    <!-- HERO -->
    <section class="hero-section" style="margin-top:76px;">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-10">
                    <h1 class="display-5 fw-bold mb-2">
                        <i class="@Model.Hero.Icon me-2"></i>@Model.Hero.Title
                    </h1>
                    <p class="lead mb-0">@Model.Hero.Subtitle</p>
                </div>
            </div>
        </div>
    </section>

    <!-- CONTENU -->
    <section class="content-section">
        <div class="container">
            <!-- INTRO -->
            <div class="row mb-5">
                <div class="col-12">
                    <div class="info-card">
                        <h3><i class="fas fa-info-circle me-2"></i>@Model.Intro.Title</h3>
                        @foreach (var p in Model.Intro.Paragraphs)
                        {
                            <p>@p</p>
                        }
                    </div>
                </div>
            </div>

            <!-- BUT ET ENJEUX (NOUVEAU) -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.ButEnjeux.Title</h2>
                    <div class="info-card">
                        <ul>
                            @foreach (var item in Model.ButEnjeux.Items)
                            {
                                <li>@item</li>
                            }
                        </ul>
                    </div>
                </div>
            </div>

            <!-- OBJECTIF + ALERTE -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.Objectif.Title</h2>
                    <div class="info-card">
                        @foreach (var p in Model.Objectif.Paragraphs)
                        {
                            <p>@p</p>
                        }

                        <div class="alert-warning-custom mt-4">
                            <h4><i class="fas fa-exclamation-triangle me-2"></i>@Model.Objectif.Alert.Title</h4>
                            <p class="mb-0">@Model.Objectif.Alert.Text</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- OBLIGATIONS DU TRAVAILLEUR (NOUVEAU) -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.ObligationsTravailleur.Title</h2>
                    <div class="info-card">
                        <p class="text-muted mb-3">
                            <i class="fas fa-book me-2"></i>@Model.ObligationsTravailleur.Reference
                        </p>
                        <blockquote class="mb-3" style="border-left: 3px solid var(--primary); padding-left: 1rem; font-style: italic;">
                            @Model.ObligationsTravailleur.Text
                        </blockquote>
                        <p class="text-muted mb-0">
                            <small><i class="fas fa-info-circle me-2"></i>@Model.ObligationsTravailleur.Note</small>
                        </p>
                    </div>
                </div>
            </div>

            <!-- OBLIGATIONS DE L'EMPLOYEUR (NOUVEAU) -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.ObligationsEmployeur.Title</h2>
                    <div class="info-card mb-4">
                        <p class="text-muted mb-2">
                            <i class="fas fa-book me-2"></i>@Model.ObligationsEmployeur.Reference
                        </p>
                        <h4 class="text-center mb-0" style="color: var(--primary);">@Model.ObligationsEmployeur.Subtitle</h4>
                    </div>

                    <div class="row">
                        @foreach (var principe in Model.ObligationsEmployeur.Principes)
                        {
                            <div class="col-lg-6 mb-3">
                                <div class="info-card h-100 d-flex align-items-start">
                                    <div class="me-3" style="min-width: 40px;">
                                        <span class="badge rounded-circle" 
                                              style="background-color: var(--primary); width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; font-size: 1.2rem;">
                                            @principe.Ordre
                                        </span>
                                    </div>
                                    <p class="mb-0">@principe.Text</p>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- CONTENU DU DOCUMENT (4 cartes) -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.Contenu.Title</h2>
                    <p>@Model.Contenu.Lead</p>

                    <div class="row mt-3">
                        @foreach (var c in Model.Contenu.Cards)
                        {
                            <div class="col-lg-6 mb-4">
                                <div class="info-card h-100 text-center">
                                    <i class="@c.Icon fa-3x mb-3" style="color: var(--primary);"></i>
                                    <h3>@c.Title</h3>
                                    <p class="mb-0">@c.Text</p>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- TYPES DE RISQUES -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.RiskTypes.Title</h2>
                    <div class="row">
                        @foreach (var r in Model.RiskTypes.Items)
                        {
                            <div class="col-lg-4 mb-4">
                                <div class="info-card h-100">
                                    <h3><i class="@r.Icon me-2"></i>@r.Title</h3>
                                    <ul>
                                        @foreach (var b in r.Bullets)
                                        {
                                            <li>@b</li>
                                        }
                                    </ul>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- MISE A JOUR -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.MiseAJour.Title</h2>
                    <div class="info-card">
                        <p>Le Document Unique doit être mis à jour dans les cas suivants :</p>
                        <ul class="mb-0">
                            @foreach (var b in Model.MiseAJour.Bullets)
                            {
                                <li>@b</li>
                            }
                        </ul>
                    </div>
                </div>
            </div>

            <!-- FORMATS -->
            <div class="row mb-5">
                <div class="col-12">
                    <h2 class="section-title">@Model.Formats.Title</h2>
                    <div class="row">
                        @foreach (var f in Model.Formats.Items)
                        {
                            <div class="col-lg-6 mb-4">
                                <div class="info-card text-center h-100">
                                    <i class="@f.Icon fa-3x mb-3" style="color: var(--primary);"></i>
                                    <h3>@f.Title</h3>
                                    <p class="mb-0">@f.Text</p>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- CONTACT 
            <div class="row">
                <div class="col-12">
                    <div class="info-card text-center">
                        <h3><i class="fas fa-phone-alt me-2"></i>@Model.Contact.Title</h3>
                        <p>@Model.Contact.Text</p>
                        <p class="mb-4">
                            <strong>Téléphone :</strong>
                            <a href="@Model.Contact.PhoneHref"
                               style="color: var(--primary); font-weight: bold; font-size: 1.3rem;">
                                @Model.Contact.PhoneDisplay
                            </a>
                        </p>
                        <a href="@Model.Contact.ButtonHref" class="btn-primary-custom">
                            <i class="fas fa-envelope me-2"></i>@Model.Contact.ButtonText
                        </a>
                    </div>
                </div>
            </div>-->
        </div>
    </section>

    <!-- ZONES + MOTS-CLÉS 
    <section class="section-padding bg-light">
        <div class="container">
            <div class="cities-cloud">
                <h3 class="fw-bold">
                    <i class="fas fa-map-marker-alt me-2"></i>Zones d'intervention — DUERP
                </h3>
                <p class="lead mb-4">@Model.CitiesIntro</p>
                <div>
                    @foreach (var c in Model.Cities)
                    {
                        <a class="city-link" href="#">@c</a>
                    }
                </div>
                <div class="mt-4">
                    <small class="text-muted">
                        <strong>Mots-clés :</strong> @string.Join(", ", Model.Keywords)
                    </small>
                </div>
            </div>
        </div>
    </section>
-->
    
    <CitiesCloud Villes="@Model.Cities"
                 AfficherMotsCles="true"
                 MotsCles="@Model.Keywords"
                 TitreSection="Zones d'intervention - DUERP"
                 SousTitreSection="@Model.CitiesIntro" />
        
    <CtaBar Title="@Model.Cta.Title" 
            Text="@Model.Cta.Text" 
            Phone="06 85 75 27 75" 
            PhoneHref="tel:0685752775" 
            ContactHref="/contact" Dark="true" />
    
    <!-- CTA FINAL
    <section class="cta-section">
        <div class="container text-center">
            <h2 class="mb-3">@Model.Cta.Title</h2>
            <p class="mb-4">@Model.Cta.Text</p>
            <a href="@Model.Cta.ButtonHref" class="btn-primary-custom">
                <i class="fas fa-edit me-2"></i>@Model.Cta.ButtonText
            </a>
        </div>
    </section> -->
}