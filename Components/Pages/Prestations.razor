@page "/prestations"
@using Coossi.Blazor.Components.Services
@using Coossi.Blazor.Components.Shared
@inject NavigationManager Nav

<SeoFromJson Id="prestations" />


<Breadcrumb Current="Prestations" />


<PageHeader Title="Nos Prestations"
                              Lead="Coordination SSI, Notices, Audits, Dossiers…" />


<ServicesGrid JsonId="prestations/_index" />

<WeightedCloud Id="prestations-generic" />

@* @code {
    IReadOnlyList<Coossi.Blazor.Data.Models.PrestationDto>? items;
    protected override async Task OnInitializedAsync() => items = await Store.ListAsync();
}

<Seo Title="Prestations | COOSSI"
     Description="Coordination SSI, dossier SSI, audit & diagnostic, DUERP, notices, RUS, signalétique."
     Canonical="@(Nav.Uri)" />

<h1 class="mb-4">Nos prestations</h1>

@if (items is null)
{
  <div class="text-muted">Chargement…</div>
}
else
{
  <div class="row g-4">
    @foreach (var p in items)
    {
      <div class="col-12 col-md-6 col-lg-4">
        <a class="text-decoration-none text-reset" href="/prestation/@p.Slug">
          <div class="card card-service h-100">
            <div class="card-body">
              <img src="@($"/images/categories/{p.Slug}.svg")" alt="@p.Title" width="48" height="48" class="mb-3">
              <h3 class="h5 fw-bold mb-2">@p.Title</h3>
              <p class="text-muted mb-0">@p.MetaDescription</p>
            </div>
          </div>
        </a>
      </div>
    }
  </div>
} *@
